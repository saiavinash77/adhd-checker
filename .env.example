# Supabase
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Groq API (used via Supabase Edge Function — never expose here)
# GROQ_API_KEY goes in Supabase Edge Function secrets only
